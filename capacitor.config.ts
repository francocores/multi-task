import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.multi.task.app',
  appName: 'multiTaskApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
